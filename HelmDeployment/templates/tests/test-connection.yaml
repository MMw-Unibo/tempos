apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "HelmDeployment.fullname" . }}-test-connection"
  labels:
    {{- include "HelmDeployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "HelmDeployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
